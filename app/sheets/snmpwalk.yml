---
:description: Generated sheet for snmpwalk
:tags:
- community
:shortcuts:
  :defaults:
    'To retrieve all of the variables under system for host zeus:': "snmpwalk -Os
      -c public -v 1 zeus system\n\n"
    'To retrieve the scalar values, but omit the sysORTable for host zeus:': 'snmpwalk
      -Os -c public -v 1 -CE sysORTable zeus system

      '
