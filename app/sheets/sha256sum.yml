---
:description: Generated sheet for sha256sum
:tags:
- checksum
- community
- cryptography
- hash
- verification
:shortcuts:
  :defaults:
    'To compute hash (checksum) of <file>:': "sha256sum <file>\n\n"
    Enter text in keyboard and finish by ctrl-D at beginning of line: |+
      sha256sum
      Some text
      ^D

      echo -n "Hello World" | sha256sum

    'print "filename: FAILED".': "sha256sum --check <file-with-list-of-checksums>\n\n"
    missing files: 'sha256sum --check --ignore-missing <file-with-list-of-checksums>

      '
...
