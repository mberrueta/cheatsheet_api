---
:description: Generated sheet for install
:tags:
- community
:shortcuts:
  :defaults:
    To create a directory for a user with a group with permission 700: 'install -o
      <username> -g <groupname> -m 700 -d /home/<username>/<directoryname>

      '
