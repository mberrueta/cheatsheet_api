---
:description: Generated sheet for unzip
:tags:
- community
- compression
:shortcuts:
  :defaults:
    'To extract an archive:': "unzip <archive>\n\n"
    'To extract an archive to a specific directory:': "unzip <archive> -d <directory>\n\n"
    'To test integrity of archive:': "unzip -tq <archive>\n\n"
    'To list files and directories an archive:': 'unzip -l <archive>

      '
