---
:description: Generated sheet for ansible
:tags:
- community
- orchestration
:shortcuts:
  :defaults:
    'To run a command on multiple instances at once - using `servers` group from `inventory.yml`:': 'ansible
      -u ansible -i inventory.yml servers -m shell -a "ls /var"

      '
